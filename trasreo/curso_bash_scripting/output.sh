# !/bin/bash

# permite decirle al comando cat que vamos a insertar algo,
# en este caso sera texto. Cuando se ejecuta el programa,
# se imprime directamente el texto. 

cat << myText

Aca colocaremos todo el texto que queremos mostrar en consol, desde la terminal
myText
